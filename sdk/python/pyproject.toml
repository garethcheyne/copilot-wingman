[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wingman-sdk"
version = "0.0.1"
description = "Official Python SDK for the Wingman chat proxy (scaffold)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "Wingman" }]
dependencies = [
  "httpx>=0.27",
]

[project.urls]
Homepage = "https://wingman.err403.com"

[tool.hatch.build.targets.wheel]
packages = ["src/wingman"]
